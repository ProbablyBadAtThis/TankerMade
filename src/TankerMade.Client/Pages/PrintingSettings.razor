@page "/modules/printing-3d/settings"
@inject TankerMade.Client.Services.ClientModuleState ModuleState
@inject TankerMade.Client.Services.AuthSession AuthSession

<PageTitle>3D Printing Settings</PageTitle>

<div class="module-shell module-shell-printing">
    <header class="module-hero">
        <h1>3D Printing Settings</h1>
        <p>Set defaults for materials, queue behavior, and workspace preferences.</p>
    </header>

    @if (!AuthSession.IsSignedIn)
    {
        <section class="module-card">
            <p>Sign in to use this module section.</p>
            <a class="btn btn-primary" href="/login">Sign in</a>
        </section>
    }
    else if (!ModuleState.IsActive("printing-3d"))
    {
        <section class="module-card">
            <p>Activate 3D Printing from Home to continue.</p>
            <a class="btn btn-outline-primary" href="/home">Go to Home</a>
        </section>
    }
    else
    {
        <section class="module-grid">
            <article class="module-card">
                <h2>Defaults</h2>
                <p>Store preferred spool units, search behavior, and entry presets.</p>
            </article>
            <article class="module-card">
                <h2>Module Profile</h2>
                <p>Prepare compatibility settings for external package manifests.</p>
            </article>
        </section>
        <a class="btn btn-outline-secondary" href="/modules/printing-3d">Back to 3D Printing Home</a>
    }
</div>
